/**
 * Podium visual constants.
 *
 * The Podium component passes these to `UserAvatar size={…}` (a number
 * prop) and the loading skeleton uses the same values so the avatar
 * circles stay pixel-identical between skeleton and real render.
 *
 * One source of truth — do not duplicate these elsewhere.
 */

export const PODIUM_AVATAR_SIZE_FIRST = 88;
export const PODIUM_AVATAR_SIZE_RUNNER_UP = 64;
