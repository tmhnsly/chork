// Twitter / X summary_large_image uses the same 1200×630 hero as OG —
// re-export the opengraph-image route so both cards stay in sync.
export { default, alt, size, contentType } from "./opengraph-image";
