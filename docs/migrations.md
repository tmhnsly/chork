# Migration catalogue

One-line-per-file so future-you (and agents) can scan the history
without opening 22 SQL files.

Apply all pending migrations: `npx supabase db push`.
Regenerate types after any apply: `npx supabase gen types typescript --project-id <id> > src/lib/database.types.ts`.

---

## Applied (in order)

| # | File | Purpose |
|---|---|---|
| 001 | `initial_schema.sql` | Core tables: profiles, gyms, gym_memberships, sets, routes, route_logs, comments, comment_likes, activity_events. Initial RLS. `handle_new_user` trigger. `get_route_grade` + `get_user_set_stats` RPCs. Yonder seed gym |
| 002 | `add_gym_id_to_route_logs.sql` | Denormalise `gym_id` onto route_logs / comments / activity_events / comment_likes. Simplifies RLS to a direct column check instead of routes→sets joins |
| 003 | `atomic_likes_and_archived_set_policy.sql` | `increment_comment_likes` RPC. Replaces comment-like read-then-write race. Blocks inserts into archived sets at RLS |
| 004 | `follows.sql` | **[Removed in 020]** follows table + denormalised counts + update trigger |
| 005 | `leaderboard_rpc.sql` | `get_leaderboard_{set,all_time,neighbourhood,user_row}`. Dense-rank over points/flashes/sends |
| 006 | `security_hardening.sql` | Cross-gym leak fixes in leaderboard RPCs. `p_set_id` must belong to `p_gym_id`. `increment_comment_likes` gym-member gate |
| 007 | `fix_activity_events_rls.sql` | Re-applies the null-route activity_events policy with the correct name (006 had the wrong name and silently no-op'd) |
| 008 | `backend_hardening.sql` | `search_path = ''` on every SECURITY DEFINER function. Explicit authenticated grants / anon revokes. `increment_comment_likes` clamps delta to ±1. Leaderboard RPCs cap `p_limit` at 100. `profiles.name` length constraint |
| 009 | `route_logs_user_index.sql` | Index on `route_logs(user_id)` — speeds up "all my logs at this gym" queries |
| 010 | `user_achievements.sql` | Persistent `earned_at` storage for badges. Badge definitions stay in TS; only the earned timestamp lives in the DB |
| 011 | `sets_ends_at_index.sql` | Composite `(gym_id, ends_at)` index. Supports the profile-page filter that hides sets ending before the user joined |
| 012 | `db_hardening_rls_indexes_constraints.sql` | Wrap every bare `auth.uid()` in `(select auth.uid())`. Drop duplicate INSERT policy on route_logs (silent archived-set bypass). Add `is_gym_admin` / `is_gym_owner` helpers. FK indexes on `profiles.active_gym_id`, `comments.user_id`, `comments.parent_id`, `comment_likes.comment_id`, `activity_events.route_id`. CHECK constraints on route.number, attempts, follower counts |
| 013 | `user_set_stats_materialized.sql` | Materialised `user_set_stats(user_id, set_id, gym_id, sends, flashes, zones, points)` with trigger-maintained aggregates. Rewrites every leaderboard RPC to read from it |
| 014 | `admin_foundation.sql` | Admin platform: `gym_admins`, `gym_invites`, `competitions`, `competition_gyms`, `competition_categories`, `competition_participants`, `route_tags`, `route_tags_map`, `push_subscriptions`. New cols on sets (name, status, grading_scale, max_grade, competition_id, closing_event, venue_gym_id), routes (setter_name), gyms (plan_tier). Helpers `is_competition_organiser`, `is_admin_of_route`. `is_gym_admin` rewritten to read from `gym_admins`. Seeds 8 route tags |
| 015 | `sets_auto_publish_schedule.sql` | `pg_cron` extension + `auto_publish_due_sets()` scheduled every 5 min — flips `draft → live` when `starts_at` passes |
| 016 | `resolve_admin_invite_rpc.sql` | `resolve_admin_invite(token)` returns the invite row plus SQL-computed `expired` / `accepted` booleans. Avoids render-path `Date.now()` and clock skew between Node and Postgres |
| 017 | `competition_leaderboard_rpc.sql` | `get_competition_leaderboard(comp_id, category_id?, limit, offset)` — aggregates user_set_stats across every set in the comp with optional category filter |
| 018 | `admin_dashboard_rpcs.sql` | 9 dashboard RPCs: `get_set_overview`, `get_top_routes`, `get_active_climber_count`, `get_engagement_trend`, `get_flash_leaderboard_set`, `get_zone_send_ratio`, `get_community_grade_distribution`, `get_setter_breakdown`, `get_all_time_overview` |
| 019 | `competition_venue_stats_rpc.sql` | `get_competition_venue_stats(comp_id)` — per-gym activity breakdown for the organiser dashboard |
| 020 | `remove_follows.sql` | Drop the follows table + related trigger + function. Drop `profiles.follower_count` and `profiles.following_count`. Replaced by crews (021) |
| 021 | `crews_foundation.sql` | `crews`, `crew_members`, `blocked_users` tables. Profile cols `allow_crew_invites`, `invites_sent_today`, `invites_sent_date`. Helpers `is_active_crew_member`, `crew_member_status`, `is_blocking`. Atomic rate-limit RPC `bump_invite_rate_limit` (10/day). Trigger `seat_crew_creator` |
| 022 | `crew_leaderboard_rpc.sql` | `get_crew_leaderboard(crew_id, set_id, limit, offset)` (ranks active members on a gym set; unranked at bottom). `get_crew_activity_feed(limit, before)` cursor-paginated union feed across the caller's crews |
| 023 | `grade_vote_clamp.sql` | CHECK (1 ≤ `route_logs.grade_vote` ≤ 30) — defence in depth against a malformed client payload storing garbage ratings |
| 024 | `push_subscriptions_hardening.sql` | Tighten RLS on `push_subscriptions` to self-only + unique `(user_id, endpoint)` — prevents duplicate rows per device and cross-user reads |
| 025 | `hardening.sql` | CHECK (`route_logs.attempts` 0..999); composite index `crew_members(user_id, status)` |
| 026 | `denormalise_community_grade.sql` | `routes.community_grade` + `grade_vote_count` cols + trigger on `route_logs` that keeps them fresh. Admin dashboard reads these denorms instead of re-aggregating every render |
| 027 | `fuzzy_climber_search.sql` | `pg_trgm` extension in `extensions` schema, GIN trigram indexes on `profiles.username` + `name`, `search_climbers_fuzzy(query, caller_id, limit)` RPC. Schema-qualified (`extensions.word_similarity()`) because SECURITY DEFINER sets `search_path = ''` |
| 028 | `user_theme_preference.sql` | `profiles.theme text not null default 'default'`. No CHECK (the theme list is app-owned; adding a palette wouldn't require a migration) |
| 029 | `crew_activity_feed_scope.sql` | Overload `get_crew_activity_feed(p_crew_id, limit, before)` — same signature with a leading crew id scopes the feed to one crew. Caller-membership gate so a stale URL can't leak another crew's feed |
| 030 | `crew_member_previews.sql` | `get_crew_member_previews(p_crew_ids, p_limit)` — batch first-N members per crew for the /crew picker avatar stacks. Replaces N per-crew round-trips |
| 031 | `crew_ownership_transfer.sql` | Tight UPDATE policy on `crews` — only the current creator can update the row, and only to an existing active member. Enables `transferCrewOwnership` |
| 032 | `push_category_prefs.sql` | `profiles.push_invite_received`, `push_invite_accepted`, `push_ownership_changed` (bool, default true). `sendPushToUsers` filters recipients by the matching column when a category is supplied |
| 033 | `notifications_log.sql` | `notifications(user_id, kind, payload jsonb, read_at, created_at)` + RLS (read/update/delete own only, no client insert). `notify_user(user_id, kind, payload)` SECURITY DEFINER helper — three kinds at launch: `crew_invite_received`, `crew_invite_accepted`, `crew_ownership_transferred` |
| 034 | `gym_admins_tighten_select.sql` | Replace `gym_admins` open SELECT policy with scoped one (self OR fellow admin of the same gym). Previously any signed-in user could enumerate every gym's admin roster from the browser |
| 035 | `crew_member_counts.sql` | `get_crew_member_counts(p_crew_ids)` — server-side `count(*) group by crew_id` for the /crew picker. Previously `getMyCrews` streamed every row and tallied client-side |
| 036 | `profile_summary_rpc.sql` | `get_profile_summary(p_user_id, p_gym_id)` — one-call RPC returning per-set aggregates (from `user_set_stats`), active-set raw logs, and gym route count. Replaces the `getAllRouteDataForUserInGym` raw-log fetch + JS aggregation on `/u/[username]`. `is_gym_member(p_gym_id)` gate, `STABLE`, `SECURITY DEFINER`, `search_path = ''` |
| 037 | `gym_stats_rpc.sql` | `get_gym_stats_v2(p_gym_id, p_set_id default null)` — single RPC returning both all-time + set-scoped `{climbers, sends, flashes, routes}`. Replaces the two-call `getGymStats` pattern that fired 8 round trips per `/leaderboard` paint |
| 038 | `profile_summary_extended.sql` | Extends `get_profile_summary` payload with `total_attempts` + `unique_routes_attempted` (gym-scoped, indexed scans of `route_logs`). Lets `ProfileStats` derive every `allTimeExtras` field from one RPC instead of falling back to `getAllRouteDataForUserInGym` |
| 039 | `leaderboard_cached_rpcs.sql` | `get_leaderboard_set_cached`, `get_leaderboard_all_time_cached`, `get_gym_stats_v2_cached`. Drops the `is_gym_member` auth gate (which blocks the `unstable_cache` pattern under service-role), granted to `service_role` only. Page-level membership check moves into the cached wrapper. Set-belongs-to-gym cross-ownership stays inside the RPC as belt-and-braces. Enables the `getLeaderboardCached` / `getGymStatsV2Cached` Layer-2 wraps for cross-viewer cache sharing |
| 040 | `notify_user_service_role_only.sql` | Revoke execute on `notify_user(uuid, text, jsonb)` from `authenticated`; grant to `service_role` only. Previously any signed-in user could call the SECURITY DEFINER RPC with an arbitrary target uid + payload — a spoofing surface. `notifyUser` helper in `src/lib/notify.ts` now uses the service client internally |
| 041 | `jams_foundation.sql` | Jams feature live tables + RLS: `jams`, `jam_players`, `jam_grades`, `jam_routes`, `jam_logs`, `user_custom_scales`, `user_custom_scale_grades`. Helpers `is_jam_player`, `is_jam_host`. Activity-bump triggers on `jam_logs` / `jam_routes`. Core RPCs: `generate_jam_code` (service-role), `create_jam`, `join_jam_by_code`, `add_jam_player`, `leave_jam`, `add_jam_route`, `update_jam_route`, `upsert_jam_log`, `get_jam_state` (single-trip hydrator), `get_jam_leaderboard`, `get_active_jam_for_user` |
| 042 | `jam_summaries_and_end.sql` | Permanent summary tables: `jam_summaries`, `jam_summary_players`. `end_jam` RPC collapses live `jam_logs` + `jam_routes` + `jam_grades` + `jam_players` into a summary + per-player roll-up and deletes the live rows in one transaction (target ~1KB per completed jam). `end_jam_as_player` wrapper enforces caller-is-player. Hourly `pg_cron` sweep via `end_stale_jams` auto-closes jams with `last_activity_at < now() - 24h`. History RPCs: `get_user_jams` (paginated), `get_jam_summary` (detail), `get_user_saved_scales`, `get_user_all_time_stats` (gym + jam union), `get_jam_achievement_context` (pair-max heuristic for Iron Crew) |
| 043 | `jam_rpcs_nullable_args.sql` | Redeclare `create_jam` / `add_jam_route` / `update_jam_route` / `upsert_jam_log` with explicit `default null` on every optional parameter. No behaviour change — just propagates nullability into `supabase gen types` output so the app layer doesn't have to coerce null → empty string at every call site |
| 055 | `jam_realtime_and_summary_gate.sql` | Add `jam_routes` / `jam_logs` / `jam_players` to the `supabase_realtime` publication + `REPLICA IDENTITY FULL` so `postgres_changes` events actually fire for the three tables `use-jam-realtime.ts` subscribes to (cures "routes / scores don't update live without refresh"). Also move the jam-summary access gate INSIDE `get_jam_summary_for_user` via the explicit `p_user_id` arg — the page used to run two anon RLS pre-flights that transiently 404'd on the fresh-redirect after `end_jam` because `auth.uid()` briefly resolves NULL through the user's JWT. Page now trusts the hydrator's null-return as "not authorised" |
| 063 | `compute_points_fn.sql` | `public.compute_points(attempts, completed, zone)` — immutable single source of truth for the scoring ladder (mirrors `computePoints()` in `src/lib/data/logs.ts`). Recreates all nine live formula-bearing functions on top of it (`get_leaderboard_*` ×4, `get_user_set_stats`, `sync_user_set_stats`, `get_user_all_time_stats`, `end_jam`, `get_jam_leaderboard`). Unifies the `attempts >= 4` vs bare-`completed` 1-point edge on the TS semantics |
| 064 | `quick_win_hardening.sql` | Audit 2026-08-10 quick wins: fixes the live `jam_summary_players` deletion bomb plus assorted low-risk, non-breaking corrections surfaced by the full audit |
| 065 | `deletion_path_integrity.sql` | Direction A. `sync_user_set_stats()` DELETE branch made update-only so it can't re-insert a stats row against an already-deleted user |
| 066 | `grant_discipline.sql` | Direction B. Replaces the legacy blanket auto-grant (full CRUD to `anon` + `authenticated`) with explicit per-table grants scoped to each table's RLS — RLS is no longer the only gate |
| 067 | `rls_tighten.sql` | Direction B. Blocks repointing a `crew_members` row into another crew via a trigger (WITH CHECK can't see OLD), plus further policy tightening |
| 068 | `comment_likes_integrity.sql` | Direction C. `comments.likes` was maintained only by the `increment_comment_likes` RPC alongside a separate `comment_likes` row — replaced with a trigger so the counter can't drift from the rows |
| 069 | `lock_new_trigger_fns.sql` | Locks down `guard_crew_member_update` (067) and `sync_comment_likes` (068) — trigger bodies fire as the table owner and are never called directly, so direct EXECUTE is revoked |
| 070 | `auto_publish_requires_routes.sql` | `auto_publish_due_sets` skips routeless drafts, mirroring the app-side guard in `updateSet`'s publish path — a scheduled draft reaching its start time with no routes would have published an empty Wall |
| 071 | `auto_archive_ended_sets.sql` | The missing counterpart to 015/070: moves `live` → `archived` once `ends_at` passes, on the same 5-minute cron. Without it a set stayed live indefinitely, so `/admin` read LIVE while climbers read SET ENDED off the same row — and `route_logs` inserts stayed open against it |
| 072 | `membership_visibility.sql` | Two own-rows-only SELECT policies that the app legitimately read *across* users, so both features silently returned nothing. `crews` widened to any membership row (a pending invitee couldn't read the crew's name, so the invite vanished from /crew); `gym_memberships` widened to `is_gym_member(gym_id)` (the target-climber checks in `fetchClimberLogs` / `fetchSetPlacement` could never pass) |
| 073 | `route_log_gym_integrity.sql` | Insert policy now also requires `sets.gym_id = route_logs.gym_id` — it verified you belonged to the named gym and that the set was live, but never that the *route* belonged to that gym, so a multi-gym climber could land a log on the wrong board |
| 074 | `neighbourhood_board_position.sql` | `get_leaderboard_neighbourhood` returns `board_position` (0-based `row_number` over `(rank, username)`, the paging RPCs' own ordering). `dense_rank()` makes rank a label not a position, so `rank - 1` drifted from the true offset past the first tie and BrowseSection cached the same climber at two offsets — the board rendered duplicate rows |
| 075 | `set_overview_time_elapsed.sql` | `get_set_overview` also returns `time_elapsed_pct`. `SetPaceWidget` computed it in React from a lazy `Date.now()` — passes `react-hooks/purity` but still runs on both server and client at different instants, so the two percentages disagreed in their far decimals and React logged a hydration mismatch on every admin load. Also compared a Node clock against Postgres's; `days_remaining` in the same RPC was already SQL-derived |
| 076 | `jam_grade_retention.sql` | `jam_summary_grades` + `end_jam` writes a per-(climber, grade) rollup before the collapse. The collapse kept only the top 5 routes *aggregated across the field*, so nothing could say which grades a given climber sent — gym sends keep full grade history forever, jam sends kept none. Not backfillable; every jam ended before this lost it permanently. Attempts deliberately absent (private by domain rule, and this table is world-readable); custom-scale jams contribute no rows since their ordinals aren't comparable, and land in `jam_summary_players.ungraded_sends` instead |
| 077 | `jam_summary_grades_grants.sql` | Revokes anon + narrows authenticated to SELECT on 076's table. A GRANT *adds* to Supabase's legacy auto-grant rather than replacing it, so the table landed with full CRUD for both roles — never exploitable (RLS on, only a SELECT policy) but wider than the policy behind it. `jam_summary_grades` was the only table in `public` with any anon privilege |
| 078 | `remap_theme_preferences.sql` | Maps stored `profiles.theme` onto the four-palette set by accent hue (slate→blue, gray→violet, mauve/sand→pink, sage→default). Free-text column with no CHECK, and the client already falls back to the default for unknown names, so this only stops a deliberate choice being silently reset |
| 079 | `result_share_token.sql` | `jam_summaries.share_token` + partial unique index. Capability for the public `/r/<token>` result page: minted app-side only when a participant taps Share, read through the service client, so nothing is granted to `anon` and unshared summaries stay exactly as private as before |
| 080 | `set_convergence_schema.sql` | **Set convergence phase 1.** `sets` can now host a climber-run Match as well as a gym Set: `gym_id` / `ends_at` / `max_grade` go nullable, `owner_kind` (`gym` \| `climber`) + `host_id` + `code` + `min_grade` added, shape enforced by `sets_owner_shape_ck`. `routes` gains `description` / `added_by` / `grade`; `route_logs` gains a denormalised `set_id` (backfilled, NOT NULL) and a nullable `gym_id`. New `set_players` table, `is_set_player()` / `can_read_set()` helpers, and every sets/routes/route_logs policy replaced by a two-branch superset (gym member **or** set player). Purely additive — `jam_*` is untouched and still read by the app |
| 081 | `route_log_set_id_trigger.sql` | Derives `route_logs.set_id` from the route on insert and on any route change, so 080's denormalised copy is never client-supplied. Migration 073 is the record of what the alternative costs: a denormalised id that policy alone had to keep honest let a log land on the wrong gym's board |
| 082 | `user_set_stats_gymless.sql` | Drops NOT NULL on `user_set_stats.gym_id`. `sync_user_set_stats()` takes that value from `sets.gym_id`, which 080 made nullable — so the FIRST Match log would have hit the constraint and aborted the whole `route_logs` insert, since the trigger runs in the writer's transaction. Not stale stats: a failed send. Verified live by round-tripping a Match through a rolled-back transaction |
| 083 | `routes_declared_grade.sql` | `routes.grade` → `routes.declared_grade`. The name came across from `jam_routes`, where it was unambiguous; on `routes`, next to `community_grade`, a bare `grade` doesn't say whether it's the input or the output of grading. Free at the time — 080 had only just added it and nothing read it yet |
| 084 | `match_rpcs.sql` | **Set convergence phase 2a.** The Match RPC layer on the converged tables: `create_match`, `lookup_match_by_code`, `join_match`, `add_match_route`, `get_match_leaderboard`, `get_match_state_for_user`, `end_match`, `end_stale_matches`, plus `sets.location` / `sets.last_activity_at`, the `set_grades` table and activity triggers. Additive and inert — nothing calls these yet, and every `jam_*` function is untouched. Two deliberate fixes vs the jam originals: the leaderboard is **gated** (`get_jam_leaderboard` had no access check), and its attempt mask resolves against a viewer the service-role path can name (the jam version masked the caller's own attempts too). `end_jam`'s 110 lines of collapse-and-delete become a status change |
| 085 | `match_history_and_sharing.sql` | **Set convergence phase 2b.** Everything that reads a Match after it ends, moved off the `jam_summaries` family: `match_standings` (one ranking, spelled once, so history / live board / shared card cannot disagree), `get_match_history`, `get_match_achievement_context`, `get_public_match_result` (no `attempts` in the return shape at all — there is no viewer to mask against on a public page), plus `sets.share_token` and realtime (`REPLICA IDENTITY FULL` + publication for routes / route_logs / set_players). Also carries the one real jam in production ("Portland Saturday") across as an archived Set rather than dropping it with the table |
| 086 | `active_match_for_user.sql` | `get_active_match_for_user` — the resume banner's "you're in a Match right now". Missed in 084 because it is the one read that is about neither running nor reviewing a Match |
| 087 | `match_route_update_with_check.sql` | Adds the missing `with check` to `set_routes_update_by_player` (080 wrote it `using`-only, so a player could repoint a route's `set_id` at a Set they have no part in — a gym's, landing a route on that gym's wall), and extends the same to `route_logs` UPDATE, whose `with check` was `user_id = auth.uid()` and nothing else. That let the owner of a log rewrite its `route_id` / `gym_id` and put themselves on the board of a gym they had never visited — migration 073's bug, reached by editing instead of inserting. Also makes archived Sets read-only on the update path, matching migration 003's rule for inserts |
| 088 | `upsert_match_log.sql` | `upsert_match_log`. Logging looked like it needed no RPC — and doesn't, except for `completed_at`, whose value depends on the row's previous state ("still completed → leave it alone"). A plain upsert restamps it, which would reorder tied climbers, since `last_send_at` is the board's fourth tiebreak |
| 089 | `drop_jam_family.sql` | **Retires the `jam_*` family.** Drops 8 tables and 21 functions, plus `get_user_all_time_stats` (the only non-jam-named function that read them; no callers). Verified empty first: the live tables held 0 rows, and `jam_summaries`' single real row was already rebuilt as an archived Set by 085. Also swaps the pg_cron entry — `end_stale_jams` off, `chork_end_stale_matches` on, closing a gap where 084 wrote the Match sweep but never scheduled it, so an abandoned Match would have stayed `live` forever. **Tables drop BEFORE functions**, which is the opposite of the intuitive order: every jam table's RLS policies depend on `is_jam_player(uuid)`, so dropping that first fails. Found by dry-running against production |
| 090 | `rename_match_badge_keys.sql` | Maps the 8 `jam-*` badge ids onto `match-*` in `user_achievements`. The rename is otherwise a pure code change, but badge ids are PERSISTED and the evaluator's "already earned" check compares against them — so renaming the config without this makes every Match badge silently unearned and re-issues it with a fresh `earned_at`, rewriting the date. Mapping is spelled out rather than derived, because `jam-first-jam` → `match-first-match` is not a prefix swap. Raises if any unmapped `jam-%` key survives |
| 091 | `discipline.sql` | Boulder / sport / top-rope. `sets.discipline` (default `boulder`) and `routes.discipline` (nullable = inherit, so changing a Set's discipline moves every route that never disagreed). Extends `grading_scale` with the rope scales `yds` and `french`. Deliberately NO check that scale suits discipline: a Set's discipline is only a default and its routes may each differ, so a mixed outdoor Match on a custom ladder is legitimate — the app offers the sensible scales at the point of choosing |
| 092 | `match_rpcs_discipline.sql` | `create_match` and `add_match_route` take a discipline, validate it, and admit the rope scales. Both drop their previous overload so there is exactly one of each rather than an unnamed-argument call resolving to the old one |
| 093 | `route_discipline_normalise.sql` | Trigger: a `routes.discipline` equal to its Set's is stored as NULL. 092 did this inside `add_match_route`, but editing a route is a direct UPDATE under RLS, not an RPC — so adding a route as "Sport" in a Sport Match inherited correctly while *editing* it to "Sport" pinned it forever. It is a property of the column, not of a code path, which is the same reasoning that put `route_logs.set_id` in a trigger |
| 094 | `grade_distribution.sql` | `get_grade_distribution(user_id)` — sends and flashes per (discipline, scale, grade), for the profile pyramid. **Replaces migration 076's storage plan**, which snapshotted per-grade counts into `jam_summary_grades` on jam end and had accrued 0 rows in its lifetime because nothing read it. Since the convergence a Match send is an ordinary `route_logs` row, so gym and Match sends roll up together, live, with no snapshot to keep in sync. No attempts in or out; no cross-scale conversion — a 6a+ never renders as a V-grade. Sends on `points` / `custom` scales collapse into one bucket the UI reports rather than silently drops |
| 095 | `guest_players.sql` | **Guest players.** A named seat in a Match with no account — no `auth.users` row, no profile, no session. `set_players` gets a surrogate PK, a nullable `user_id` and a `display_name`; `route_logs` gets a nullable `user_id` and a `player_id` naming the seat, with `route_logs_owner_ck` enforcing exactly one. Only the HOST may seat a guest or write their log, which is the anti-cheat: a self-reporting account would be trivially minted by anyone holding the code. Also skips `sync_user_set_stats` for guest logs — second time that trigger has caught a widening (082 was the first), because it is a cache keyed on an ACCOUNT and any identity that isn't one has to be skipped rather than accommodated |
| 096 | `match_reads_with_guests.sql` | Match reads group by SEAT rather than account, so guests appear on the board with their name resolved from the seat instead of a profile. `match_standings`, `get_match_leaderboard`, `get_match_state_for_user`, `get_match_history` and `get_public_match_result` all follow. Adds `guest_logs` to the room bundle, host-only — the host entered them and is the one person for whom they aren't someone else's private data. **Scope is Matches only**: a guest's log has no `gym_id` and belongs to a climber-owned Set, so the gym leaderboard cannot see one by construction, which an integration test asserts |
| 097 | `upsert_guest_log.sql` | `upsert_match_log` gains `p_player_id` so the host can enter a guest's send with the same `completed_at` care as any other log — a guest is exactly as entitled to keep their place in a tie. Note the `ON CONFLICT` has to repeat the partial index's `where player_id is not null` predicate, or Postgres can't match it |
| 098 | `handicap.sql` | Handicap scoring for Matches. `sets.handicap` (CHECKed to climber-owned Sets only), `set_players.ceiling` in the Match's own scale, and `handicap_multiplier` / `handicap_points_tenths` mirroring `src/lib/data/handicap.ts`. The taper is 1 / 0.7 / 0.4 then **nothing** — the cutoff is the balance mechanism, measured rather than guessed: with any tail the stronger climber wins on volume (ratio 1.43 with a 0.1 floor, 1.00 with the cutoff). Above-ceiling is capped at full value, never bonused, or declaring low beats being honest. Pinned to the TS by `scoring-parity.test.ts`, mutation-tested both ways |
| 099 | `standings_handicap.sql` | The board reads the handicap. `match_standings` applies the multiplier when `sets.handicap` is on and ranks by the result; totals come back in TENTHS as integers (exact sums, no float drift) alongside the unchanged base `points`. With the handicap off `points_tenths = points * 10`, so ranking is one clause for both modes rather than a branch. `get_match_leaderboard`, `get_match_state_for_user`, `get_match_history` and `get_public_match_result` all follow |
| 100 | `match_settings.sql` | **A host could not change anything about their own Match** — `sets` had exactly one policy, a SELECT, so turning the handicap on silently updated zero rows. Deliberately NOT fixed with an UPDATE policy: `sets` holds `owner_kind`, `gym_id`, `status` and `share_token`, so a policy broad enough to flip `handicap` would also let a host convert their Match into a gym Set or mint themselves a share token. Narrow definer functions instead — `set_match_handicap` and `set_match_ceiling` (your own, or a guest's if you host) — plus `p_handicap` on `create_match`, which refuses a scale with no grades |
| 101 | `ceiling_nullable_arg.sql` | `set_match_ceiling` could not clear a limit — the `drop function` that followed a `create or replace` matched the function just written, because a changed DEFAULT is not a changed signature |
| 102 | `leaving_parks_the_seat.sql` | **Leaving a Match erased you from it.** `match_standings` filtered `left_at is null`, so a climber who left lost their points from the board *and* from the final result their mates had watched them earn; the viewer gates did the same, so they also lost access to it. Same trap gym memberships avoid. One helper was doing two jobs, so it is now two: `is_set_player` (a seat, parked or not) gates READS, new `is_active_set_player` gates WRITES. Standings and both read RPCs return `has_left` so the row can be marked instead of ranking a ghost. Also puts `sets` in the realtime publication, so ending a Match can reach other open screens |
| 103 | `write_rpcs_use_the_write_gate.sql` | **102 repointed every policy and none of the three SECURITY DEFINER functions** — which bypass RLS and carry their own check, so no policy was ever going to cover them. Between the two, a climber who had left could still log, add routes and end the Match through the RPCs the app actually calls; 102's test missed it by exercising a direct INSERT instead. Bodies regenerated from `pg_get_functiondef` with one line changed each, after a hand-copy silently dropped `upsert_match_log`'s guest branch. Also: **`end_match` was never host-only** — any player could end it for everyone, and the menu offered them the button |
| 104 | `friends_graph.sql` | The friends graph — one row per pair, unique on the *unordered* pair via `(least, greatest)`. **No Data API grant**: every read and write is a SECURITY DEFINER RPC, so the table is unreachable from supabase-js and RLS is not the only gate. The RPC owns the state machine — idempotent asks, asking-back-accepts, silent declines, and only the person who declined may revive a declined link. Suggestions come from shared Matches, never gym Sets (everyone shares the current Set; that would be a directory) |
| 105 | `mate_notification_kinds.sql` | Two notification kinds for the graph. Superseded by 106's rename |
| 106 | `mates_to_friends.sql` | Mates → Friends, in the database as well as the UI. A table called `mates` behind a screen called Friends is the drift `follower_count` already taught us about, and it was hours old with zero rows. Function bodies re-created rather than `alter function … rename`, because a body is stored as text and re-resolved at call time |
| 107 | `friends_leaderboard.sql` | The board crews were actually for. Same shape as `get_crew_leaderboard` — set-scoped, `user_set_stats`, points then flashes then sends — with membership rows replaced by "you, plus everyone you're linked to". Landed BEFORE crews were dropped so the private leaderboard was never missing. Always includes the caller: a board of your friends without you is a list of other people |
| 108 | `remove_crew.sql` | **Crews removed.** Tables first so cascade takes their triggers and policies, then every `%crew%` function enumerated rather than listed (`get_crew_activity_feed` had two overloads — a hand-written list is how you leave one behind). `allow_crew_invites` → `allow_friend_requests`, and **finally enforced**: `request_friend` never consulted it, so it was a privacy switch that did nothing. The refusal is indistinguishable from "no such climber", so a stranger can't probe. `invites_sent_today/date` dropped — crew's own quota; friend requests use the standard mutation rate limit |
| 109 | `friend_moments.sql` | Moments — the only surface that shows you a friend at a different gym, since the board is set-scoped and two friends who share no Set share no board. Four kinds, all DERIVED at read time: new personal best (partitioned by discipline AND grading scale — a V5 and a font 5 are different climbs, so comparing them would invent a best out of a change of notation), won a Match, earned an achievement, podium in a gym competition. Returns a **date, not a timestamp**: the coarse-timestamp privacy rule is enforced in SQL rather than the renderer, so one careless component can't leak when a friend is at the gym |
| 110 | `moments_one_best_per_day.sql` | Found by looking at the feed: a climber working up V2 → V3 → V4 → V5 in one session produced FOUR moments, three of them the rungs on the way. Each was individually true; together they reported the working as well as the answer. `distinct on` keeps the hardest per (climber, discipline, scale, day). Per DAY because a day is all the feed shows — grouping by session would need the clock times 109 deliberately throws away |
| 111 | `chork_game_mode.sql` | Chork — HORSE on a wall, as a LENS on a Match exactly like `handicap`: same container, routes, players and logs, different win condition. That is what "one engine" meant. Nothing new is stored — a round IS a route plus the log its adder left on it, and a letter IS a round you failed, so a mis-tapped attempt corrects itself instead of leaving a letter behind. `chork_allowance` and `chork_is_letter` were written as mirrors of TS twins in `src/lib/data/chork.ts`; 113 deleted those twins, because unlike `computePoints` they needed another climber's private attempt count and so could never run on a client. Note the letter rule's shape: safe means sent WITHIN the allowance, so a climber who burns their goes and tops out on the next attempt cannot erase the letter they already earned |
| 112 | `chork_concede.sql` | Giving up on a round. Letters are derived from attempts reaching the allowance without a send — which works while you're pulling and leaves nothing to say "I'm done", so a climber who walks away sat unresolved and the round never closed. Conceding reaches the same derivation deliberately rather than storing anything new. Server-side because the allowance is the SETTER's attempt count plus the ceiling bonus, and raw attempts are private to their owner — the client cannot work out how many goes it has. Works for a guest when the host acts, same split as logging. `greatest(attempts, allowance)` on conflict so conceding never hands back a letter someone already earned by pulling past it |
| 113 | `chork_pen.sql` | **Whose turn it is to set was computed on the client and wrong twice over.** It needed to know whether the setter had SENT their own challenge — their attempt count, private to them — so every viewer who wasn't the setter read each round as unsent and the pen rotated to the wrong climber. And it compared `routes.added_by` (a USER id) against a list of SEAT ids, so the lookup never matched even for the setter; that is the third time those two identifiers have been swapped. The pen is now a `has_pen` column on `chork_standings`, worked out where the numbers legally live. `drop function` before `create or replace` because an added OUT column changes the row type — the same class of trap as 101 |
| 114 | `chork_withdraw.sql` | **The pen left you the instant you set a route.** 113 read an unsent challenge as a failed one — right for a finished round, wrong for one in progress, and every route is unsent for the minute between putting it up and pulling on it. The missing state was the setter who tries their own set and can't do it: `chork_concede` refused them outright, assuming a setter always succeeds, so they had no way to end their turn and the rule had to guess. They can now take it back. **Withdrawn, not deleted** — the pen is DERIVED from the routes, so deleting the only evidence you had a go hands the pen straight back to you; the route stops being a round and leaves the wall, the row stays as the record of whose turn it was. Found by playing a game, not by reading the SQL |
| 115 | `withdrawn_leaves_the_grid.sql` | 114 taught the pen about withdrawal but the room still served the route — `get_match_state_for_user` selects every row for the set, so a challenge its setter had taken back sat on the grid, tappable and forever unanswerable. One line, regenerated from `pg_get_functiondef` rather than hand-copied, because that is how 103 silently dropped a whole guest branch out of a function it was editing one line of |
| 116 | `routes_remember_the_seat.sql` | **A guest could never take a turn.** `routes.added_by` is a user id and a guest hasn't got one, so a route the host put up on a guest's behalf recorded the HOST as its setter and the pen bounced straight back — in a match with one guest, the host set every single round. Fourth time these two identifiers have collided (102's standings, 112's log sheet, 113's pen), so the fix is to stop translating: `routes.added_by_player` records the seat, and Chork is now expressed **entirely in seats** — setter, answerer, pen and rotation all compare seat to seat, with no user id anywhere in the standings. The same collision was in `chork_concede`'s "you set this one" guard, and in `chork_round_allowance`, which looked for the setter's ACCOUNT's send and so read every guest-set round as never set. `add_match_route` gains `p_player_id` (host acting for a guest, same split as logging) and the old 5-arg signature is dropped rather than left as an overload — 101's lesson |
| 117 | `a_mixed_day.sql` | **Boulders and ropes on the same day.** A route could always override its discipline, but a Match carried exactly ONE grading scale — so switching a route to Top rope made the grade picker vanish and forced the route ungraded. `V4` is not a rope grade, so refusing was right; having nothing to offer instead was the bug, and it left half a mixed session with no grades, which silently disables the handicap and Chork's allowance for those routes. Sport and top-rope grade identically, so the ceiling is TWO scales and never three: `alt_grading_scale` + range covers the family the Match's own discipline is NOT. Named `alt_` rather than `rope_` because a Sport Match adding boulders needs the same slot the other way round. `create_match`'s 9-arg signature dropped rather than overloaded (101's lesson) |
| 118 | `a_ceiling_has_a_scale.sql` | 117's consequence: `set_players.ceiling` is one number stated in the Match's own scale, so measuring an off-family route against it compares two ladders that happen to share an integer — a 6b rope and a V6 boulder are both ordinal 6, and a V4-ceiling climber would be scored on that rope as if it were two grades above a limit they never gave. Both places that compare a route to a ceiling (`match_standings`' handicap, `chork_round_allowance`) now read an off-family route as "limit unknown", which each already handles: full value, no bonus. Same rule 111 stated for an ungraded route. The dry-run initially proved nothing — both test routes sat ABOVE the ceiling, where the multiplier caps at ×1 either way — and rewriting it below the ceiling immediately exposed that `chork_round_allowance` had been missed |
| 119 | `pyramid_reads_the_route_scale.sql` | The last place 117 reached. `get_grade_distribution` grouped every send by the SET's scale whatever discipline the route was, so on a mixed day a 6b top-rope send landed in the profile's Top rope pyramid labelled "V6" — a French 6b and a V6 are both ordinal 6. Same fix as the client-side `makeRouteLabeller`: resolve the route's family first and read the alternate scale when it differs. Found by grepping every `SCALE_LABEL` / `makeGradeLabeller` consumer after the fact, rather than by anything failing |
| 120 | `join_preview_both_scales.sql` | `lookup_match_by_code` returned one scale, so a mixed Match (117) previewed as "V-scale" to someone deciding whether to join — under-reporting, not misleading, but the preview exists so nobody has to guess. Now returns both. Regenerated from `pg_get_functiondef`; return type changed, so the old signature is dropped first (113's trap) |
| 121 | `two_limits_on_a_mixed_day.sql` | 118's honest interim — an off-family route reads as "limit unknown" — left a climber on a mixed day handicapped on half their session. A climber genuinely has two limits (V4 on the boulders, 6b on the ropes), so `set_players.alt_ceiling` pairs `ceiling`, named for the scale each is measured against, and both are set in ONE call so a save can't leave them disagreeing. The 3-arg `set_match_ceiling` is dropped, not overloaded (101's lesson) |
| 122 | `oauth_brings_a_name.sql` | `handle_new_user` reads Google's `full_name` / `picture` (and the `name` / `avatar_url` spellings other providers use) out of `raw_user_meta_data` into `profiles`, so an OAuth climber doesn't retype their own name at a gym. **Broke every email signup** — see 123 |
| 123 | `signup_was_broken.sql` | 122 passed `nullif(trim(...), '')` into `profiles.name` / `avatar_url`, which are NOT NULL with `''` defaults; every email signup (no metadata) inserted NULL, the trigger raised inside the `auth.users` INSERT, and signup rolled back with a generic "something went wrong". Fixed by landing on the column's own empty string. **Why nothing caught it:** the dry-run tested the extraction expressions in isolation and never ran the INSERT; `new-user.test.ts` pinned the function's TEXT and every pin passed with signup dead. The dry-run for this one inserted a real `auth.users` row inside a rolled-back transaction, and the test gained the one text pin that would have failed — every `nullif` wrapped in a `coalesce` — knowing what a text pin is worth |
| 124 | `friend_status.sql` | `is_friend` answers a boolean, which collapses "not asked", "waiting on them", "waiting on ME" and "they said no" into one word; the profile's action row needs four different answers. `friend_status(uuid)` returns `none / sent / received / friends / declined_by_me` — asymmetric on purpose, per 104: a decline reads as `none` to the person declined, so nothing leaks the refusal |
| 125 | `friend_status_carries_the_row.sql` | 124 told the profile it was in `received`; the button for that is Accept, and `respond_to_friend` takes the REQUEST id, not the climber's. Returns `(status, friend_id)` as a row — `friend_id` null in every state with nothing to respond to, and still hidden from the person declined |
| 126 | `suggestions_know_why.sql` | Suggestions had one source (shared Matches) and gave no reason. Adds friends-of-friends (active links on both hops only, so a pending or declined edge can't leak through a mutual count) and returns `reason` + `reason_count` so a row can say "2 mutual friends" or "climbed together". Still never gym Sets (104's rule). Best reason per person; mutual friends outranks a shared match |
| 127 | `search_climbers.sql` | Deletes `search_climbers_fuzzy`, **broken since 108** — it read `allow_crew_invites`, which 108 renamed, so every call raised 42703; 108's sweep enumerated functions by `%crew%` in the NAME and this one never said crew. `search_climbers(text)` replaces it: trigram + ilike, exact-handle match first, `allow_friend_requests` enforced (a privacy switch the search ignores is decoration), guests never appear, and linked people are RETURNED with their status because search is lookup where suggestions are discovery |
| 128 | `moments_placements.sql` | Two moment kinds the feed was missing: `set_placed` (podium on an ARCHIVED set — a live board reshuffles daily and a moment that un-happens is worse than one that arrives late) and `match_placed`, which replaces `match_won` with the rank in the detail (a match podium needs someone finishing below you: 2nd of 2 is "played"). Podium-only, the rule competitions already follow |
| 129 | `match_invite_kind.sql` | One line: the `match_invite_received` notification kind. The profile's "Start a match" read as doing something TO someone and had nothing they could decline; an invite is a message carrying the join code, not a seat — they join by `join_match` on their own action, so an ignored invite leaves no trace and there is nothing to inflict but a notification, which the existing push opt-out and the sender's rate limit already cover |
| 130 | `notify_user_knows_the_kinds.sql` | **Friend-request notifications had been failing since 108.** `notify_user` (040) kept its OWN allow-list of kinds beside the table's check constraint, still naming the three CREW kinds; every call since 108 raised "unknown notification kind", `notify()` caught it and logged a warn, and push (which goes straight to web-push) still worked — so nobody noticed the in-app list was empty. Found testing the match invite. Fix is to DELETE the function's list, not update it: the constraint is the one place a kind is allowed |
| 131 | `achievement_activity.sql` | `get_achievement_activity(uuid)` — when the caller's ladders last moved (last flash, last send, last archived match). The profile shelf became fixed slots and Tom's rule for filling them is RECENCY of activity, not proximity to the target: a badge nudged yesterday at 3/50 outranks one untouched at 49/50. Progress is a count with no timestamp, so the "contributed towards" half is derived from the climber's own rows; six progress keys collapse to three dates |
| 132 | `achievements_keep_their_clock_times.sql` | **Three things the pre-merge review of 131 pulled loose.** (1) 131's `get_achievement_activity(p_user_id)` took a caller-supplied uid, checked nothing, and returned exact `timestamptz` — the profile called it for every profile with the OWNER's id and the result rode into a client component, so a stranger's page load carried "on the wall at 14:32 today". Now no parameter (self by construction) and DATES. (2) `user_achievements.earned_at` had been readable by every authenticated user since 010 (`using (true)`), and a badge is earned by a send, so it IS a send time: own-rows-only now, and everyone else reads the DAY through `get_earned_achievements`. (3) 010 said writes are service-role and created no INSERT policy, but `completeRoute` passed the CLIMBER's client to the evaluator from the day the feature landed — RLS refused every upsert, the evaluator swallowed it, and the wall's badge toast never fired once; the profile derives earned state live so nobody saw. App-side fix; recorded here because this is where the refusing policy lives. Dry-run as authenticated + anon before push |
| 133 | `league.sql` | **League** — `leagues` (host, name, ended_at) + `sets.league_id`. The table is never stored: `league_standings` reads each archived week's own board (`match_standings`, or `chork_standings` ranked by letters), pays a fixed ladder (10/8/6/5/4/3/2/1+, tied places share the higher), drops the lowest 1 from 4 weeks and 2 from 8 (`league_drops`), ranks by points → 1sts → 2nds → 3rds. Guests place but get no row. No Data API grant — RPC-only like `friends`: `create_league`, `rename_league`, `add_match_to_league`, `remove_match_from_league`, `end_league`, `league_standings`, `get_league`, `get_my_leagues`. `create_match` gains `p_league_id` (host of a running League only) |
| 134 | `league_placings.sql` | **Fix round on 133.** Chork's "fewest letters, out sinks to the bottom" ordering was open-coded twice and had already drifted: `get_league`'s `winner_user_id` filtered `user_id is not null` BEFORE ordering, so a guest who won a Chork week was silently reported as a runner-up win. Separately, `match_standings` / `chork_standings` return a row for every seat including one that never opened a route, and `league_standings` only excluded a null `user_id` — an empty seat in a 2-player week banked 2nd place and 8 points for doing nothing. Fix is one new internal helper, `league_week_placings(set_id)`, the single home for a week's board: game-mode branch, Chork's ordering, and a `route_logs` existence check (not attempts/sends, which are masked) that drops any seat that never logged. Both `league_standings` and `get_league` now read it instead of hand-rolling their own copy. Also: `league_placement_points` / `league_drops` gained `parallel safe` and an explicit `grant … to public`, the `compute_points` (063) precedent |

---

## Conventions

- **Filename pattern**: `<zero-padded-number>_<snake_case_summary>.sql`
- **Every new table gets RLS enabled in the same migration that creates it.**
  No "add RLS later" migrations — they're a guaranteed foot-gun
- **Every new table reached from the client gets an explicit Data API
  grant in the same migration.** Supabase is retiring the auto-grant
  that exposed new `public` tables to `anon` / `authenticated` —
  **enforced on this project from 2026-10-30** (existing tables keep
  their grants; only tables created after the cutoff are affected).
  The table-level `GRANT` decides whether a role can reach the table at
  all via supabase-js / realtime; RLS only filters *rows* once it's
  reachable — they're independent layers. Without the grant a new table
  fails with `42501 permission denied` / `PGRST205 "Could not find the
  table … in the schema cache"`, and realtime delivers no events. Scope
  the grant to the table's RLS policies — no project table targets
  `anon` (public reads go through service-role `*_cached` RPCs), so the
  default is `authenticated` only:
  ```sql
  grant select, insert, update, delete on table public.thing to authenticated;
  ```
  Exceptions: a table touched **only** inside SECURITY DEFINER RPCs
  (never `.from(...)`, never a realtime subscription) needs no table
  grant — the definer runs as owner. A realtime-subscribed table needs
  at minimum `grant select … to authenticated`. This mirrors the
  function rule below: functions are already future-proof because every
  one ships an explicit `grant execute`; tables now need the same care
- **Every FK column gets an index.** Supabase lint 0001
- **Every SECURITY DEFINER function sets `search_path = ''`.** Prevents
  schema injection. Every new one must also have explicit
  `grant execute ... to authenticated` + `revoke ... from anon, public`
- **RLS expressions wrap `auth.uid()` in `(select auth.uid())`.**
  Postgres caches it once per query instead of re-evaluating per row
- **Destructive changes land in their own migration.** Adding columns
  and dropping columns mix poorly; roll each separately so partial
  applies stay recoverable
- **Backfill inline with DDL where possible.** Migration 002 is the
  canonical example — add column nullable, `UPDATE … FROM` to
  backfill, then `ALTER TABLE … SET NOT NULL`

---

## Running migrations

```bash
# Staged apply — preview what's about to run
npx supabase db push --dry-run

# Apply
npx supabase db push

# Then always regenerate types
npx supabase gen types typescript --project-id cfyagiwtzrgfjtwaevlh \
  > src/lib/database.types.ts
```

### Apply with `db push`, never the dashboard

Pasting a migration into the dashboard SQL Editor applies the DDL but
stamps the history table with its own timestamped version, which no
local file can match. Migrations 064–070 went that way, leaving seven
orphan remote rows and seven local files the CLI thought were pending —
`db push` then refused to run at all with `LegacyDbPushMissingLocalError`.

If it happens again, the two halves are reconciled with (no schema
change — this only rewrites the history table):

```bash
# Tell it the local files are already applied
npx supabase migration repair --status applied 064 065 066

# Drop the orphan dashboard-stamped rows
npx supabase migration repair --status reverted 20260810111645

# Confirm: every row should have both a local and a remote version
npx supabase migration list
```

Pair orphan rows to local files by time before repairing — the remote
timestamp is UTC at apply, so it lands seconds-to-minutes before the
matching commit in BST. Don't rely on the counts matching.

If types are missing a column / RPC, the TS compiler will fail at the
call site rather than a cryptic runtime error. That failure is the
cue to regenerate types.

---

## Rollback

We don't ship down-migrations. If a migration needs reverting:

1. Write a new forward migration that reverses it explicitly
2. Name it with the next number
3. Note it in the table above referencing the migration it reverses

Rationale: down-migrations are almost never tested and the production
database doesn't need two paths to manage. Forward-only keeps the
history honest.
